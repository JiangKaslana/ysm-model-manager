# v1.13.0 — 虚拟滚动 + 架构文档重构 + 代码健康度治理

**发布日期：** 2026-08-21

---

## 🎉 贡献者

| 贡献者 | 改动 |
|--------|------|
| **@eghrhegpe** | 虚拟滚动、架构文档重构、代码健康度治理、知识卡修正、大量审核反推修复 |

---

## 🚀 核心：工坊模型列表虚拟滚动

社区上线后仓库模型索引可顶到 2000 级，全量渲染 DOM 会爆炸。

| 改动 | 说明 |
|------|------|
| 共享原语下沉 | `utils/dom/virtual-scroll.ts`：`calcVisibleRange` + `installScrollSync` 从 app-tree 下沉，加 `topOffset` 参数 |
| 通用组件 | `community/virtual-list.ts`：定高虚拟列表（零高度自动全量回退、阈值 60 行以下直接全量、`destroy()` 移除滚动监听） |
| 接入工坊 | `renderModelList` 走虚拟列表，行高 42px 定高，jsdom 零高度自动全量回退 |
| 全选修正 | 虚拟化后 DOM 只含可见行，全选逻辑改遍历 `currentFiltered` 筛选结果 |

**设计决策**：零高度回退让既有 12 个集成测试零语义破坏，只需把 `replaceChildren(renderList())` 改成 `renderList()`。

---

## 📚 架构文档重构

| 改动 | 说明 |
|------|------|
| §7 预览系统扩充 | 从 6 行概述 → 9 个子节（统一核心 / 适配器矩阵 / 能力注册表 / 多模型同框 / 感知层 / KTX2 / 物理 / 骨骼工具链 / 2D+缓存+截图） |
| §6.5 目录更新 | `utils/3d/model3d` 25.8KB → `3d/` 含 adapters + caps + perception 共 138 文件 |
| §14 变动记录 | 新增 5 条近期变动（ADR-052 P2 删除 → 感知层 → ADR-066 → ADR-073 → ADR-093） |

---

## 🧹 代码健康度治理

### 死依赖清理

| 改动 | 说明 |
|------|------|
| `@loaders.gl/textures` | 声明了但未使用（KTX2 走自研 WASM 路径），移除 23 个包，-216 行 |
| 后处理导入统一 | `three/examples/jsm/` → `three/addons/`，3 文件 14 处 |

### 死代码清理（~2600 行）

| 改动 | 说明 |
|------|------|
| utils/core 14 个模块 | 移植自 MikuMikuAR 的零件库，业务场景未至（virtual-grid / keyboard-nav / safe-call / math-geometry / reactivity 等） |
| utils/3d 3 个模块 | 架构换代孤儿（render-loop / session-state / resource-registry） |

### 后处理双轨合并

| 改动 | 说明 |
|------|------|
| PostprocessingManager 退役 | 被 PostprocessingCapability 完全替代但没删干净，删除 115 行类 + 430 行测试，-707 行 |
| 统一 Capability | `mount-preview-core.ts` / `cleanup-3d.ts` 改 import |

### 纹理泄漏修复

| 改动 | 说明 |
|------|------|
| fbx-adapter disposeMaterial | 原只释放 3 个纹理 key（map/emissiveMap/normalMap），遗漏 roughnessMap/metalnessMap 等 6 个 → 改用 mesh.ts 权威版本 |
| safeDisposeMat 去重 | 与 mesh.ts disposeMaterial 完全重复，删除 |

### 边界安全前缀匹配

| 改动 | 说明 |
|------|------|
| loader.ts | `startsWith(normRoot)` → `startsWith(normRoot + "/") || === normRoot`，防 `PMX` 命中 `PMXSub` 误判 |
| log.ts 化石注释 | 删掉对已死 `safe-call` 的引用 |

---

## 📖 知识卡健康度排查

| 改动 | 说明 |
|------|------|
| model3d.md | 4 处修正：`buildCubeGeometry` → `buildCubeMeshData`、debugMode 接口描述、`switchTo` → `switchToSession`、screenshot-renderer 路径 |
| preview_core.md | 1 处修正：`switchTo` → `switchToSession` |
| community-feature.md | 虚拟滚动接入描述（概览/职责/API/不变量） |
| utils-dom.md | 加虚拟滚动原语描述 + 不变量 + 相关测试 |
| app-content.md | 提一句工坊虚拟滚动 |

---

## 🛠 审核反推与修复

大量子代理审核反推修复，涵盖：

| 域 | 关键修复 |
|----|---------|
| go/ysm | texsize 死循环合并 + parse.go int64 溢出防线 + decode_inject 数据竞争根治 |
| go/litematic | palette 空气判定真缺陷根治 |
| go/fileops | MoveModelFile 自嵌套检查顺序 + 禁用条目预览真缺陷 |
| go/download | Windows fd 泄漏根治 + 回收站隔离 |
| go/threejs | parseFaceUV 伪成功跳过回退根治 |
| app-content | hover 气泡竞态泄漏 + 魔数提取、workshop 进页双重 load 合并 |
| app-preview | WASM 解码静默吞错改留痕 + 纹理头魔数提取常量 |
| app-tree | 搜索幽灵状态修复 + rAF 清理 |
| app-resource-manager | 测试补盲 |
| app-sync-manager | 按钮禁用态 + 卸载守卫 + 重入守卫测试 |

---

## 🏗 工程治理

| 改动 | 说明 |
|------|------|
| funcmap 刷新 | 导出符号索引同步 |
| deadcode baseline | 更新已知项 |
| 知识卡 source_files | 修正漂移 |

---

## 📊 统计

- **提交数：** 200+（自 v1.12.0）
- **净减代码：** ~3300 行（死代码清理 + 后处理合并）
- **新增测试：** 虚拟滚动 6 用例 + 知识卡修正

## 🔗 关联 ADR

| ADR | 状态 | 说明 |
|-----|------|------|
| ADR-040 | ✅ 持续落地 | ≤400 行红线（后处理合并 / 死代码清理） |
| ADR-066 | ✅ 已采纳 | 统一预览核心 mount3D |
| ADR-073 | ✅ 已采纳 | 场景能力注册表 |
| ADR-093 | ✅ 已采纳 | 多模型同框 sceneRegistry |
