# v1.13.2 — 导入/启禁/去重修复 + 并行化与缓存优化

**发布日期：** 2026-08-24

---

## 🎉 贡献者

| 贡献者 | 改动 |
|--------|------|
| **@eghrhegpe** | 导入路由、启禁修复、dedup 并行化、纹理缓存淘汰、死代码治理、知识卡漂移清理 |

---

## 🛠 修复（重点）

| 改动 | 说明 |
|------|------|
| 禁用态文件消失 | `ScanModelEntriesFiltered` 保留 `.disabled` 文件——修复禁用资源/模型从仓库树消失 |
| 禁用图标错变 🧊 | `fileIcon` 取扩展名前先剥禁用后缀，禁用资源包图标不再错判为冰块 |
| 启禁统一口径 | 无 rtype 时纯路径判定走 `ToggleEnable`，修复资源包启禁 |
| 导入中性上下文 | ysm.json 顺序无关优先 + `ImportModelFolderTo` 缺失降级 warn |
| 拖拽导入上下文 | DnD getter 化防闭包残留 + 绑定存在性守卫 + 中立默认页让位内容推断 |
| 下载自动重试 | 同一 URL 网络类/5xx 退避重试（默认关闭，三级回退不叠加） |

---

## 🚀 性能

| 改动 | 说明 |
|------|------|
| dedup 并行化（ADR-119） | 共享并行哈希管道，确定性输出不变 |
| dedup size 预分组 | 跳过唯一尺寸文件哈希，收窄大文件长尾 |
| 扩展名集合缓存 | `IsSupportedExt/ShouldHashExt` map 缓存（atomic.Value，随注册表实例重建） |
| texture_cache 淘汰 | 容量上限 + TTL + 写路径限频触发 |
| 多模型同框排开 | X 轴自动排开，避免重叠 |

---

## 🧹 工程治理

| 改动 | 说明 |
|------|------|
| 删除悬空组件 | `app-resource-manager` 退役，连带清理其 4 个 testid + 14 个 i18n key × 3 语言 |
| 死代码基线按归属裁剪 | 新增项 ∩ staged/未推送改动才阻断，他人遗留自动收编基线留痕（含解析失败 flag 守卫） |
| 知识卡漂移清理 | 27 + 3 + 5 张卡移除硬编码行号，候选卡补 invariant_anchors，go-download/go-dedup 同步 ADR-119 语义 |

---

## 📊 统计

- **提交数：** 29（自 v1.13.1）
- **净增能力：** dedup 并行哈希、纹理缓存淘汰、下载自动重试、拖拽导入路由
- **净减：** 悬空组件 app-resource-manager + i18n 死 key + 硬行号漂移

## 🔗 关联 ADR

| ADR | 状态 | 说明 |
|-----|------|------|
| ADR-119 | ✅ 已采纳 | dedup 并行哈希管道 |